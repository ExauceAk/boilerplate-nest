# 🛠️ Backend API – NestJS

REST API with JWT authentication and simple CRUD.

## 🔧 Prérequis

- Node.js v18+
- PostgreSQL
- NestJS CLI (`npm i -g @nestjs/cli`)

## 🚀 Lancer l’API

```bash
cd backend-api
npm install
cp .env.example .env
npm run start:dev
